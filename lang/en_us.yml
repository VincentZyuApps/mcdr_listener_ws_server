mcdr_listener_ws_server:
  help:
    view_image: "View a remote image"
  command:
    player_only: "This command can only be used by players"
    image_handler_not_initialized: "Image handler is not initialized"
  player:
    leave_broadcast: "Bye {player}"
  image:
    entry_label: "[img:{idx}]"
    entry_label_plain: "[img:{idx}]"
    click_hint: "Click to view image"
    url_label: "URL: "
    blocked_by_whitelist_prefix: "Image blocked by whitelist: "
    loading: "Loading image..."
    download_failed: "Failed to download image"
    gif_first_frame: "GIF detected, displaying the first frame"
    process_failed: "Failed to process image: {error}"
    rendering: "Rendering image ({width}x{height} = {pixel_count} pixels)..."
    rendered: "Image displayed (will disappear after {duration_sec}s)"
    default_summary: "Image"
    cooldown: "!!view_image on cooldown, {remaining}s remaining"
    entry_label_hash: "[Image#{idx}]"
  log:
    write_failed: "Failed to write log: {error}"
    read_failed: "Failed to read log: {error}"
  remote_command:
    disabled: "Remote command execution is disabled by MCDR plugin config (set enable_remote_exec_command: true in config/mcdr_listener_ws_server/config.yml)"
    not_in_whitelist: "Command not in whitelist: {command}"
    rcon_returned_none: "RCON returned empty result, please check if RCON is enabled on the Minecraft server (enable-rcon=true in server.properties)"
    timeout: "Command execution timed out ({timeout_sec}s)"
